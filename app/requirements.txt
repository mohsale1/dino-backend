# FastAPI and Server
fastapi==0.115.12
uvicorn[standard]==0.47.0

# Pydantic
pydantic==2.13.4
pydantic-settings==2.14.1

# Authentication
# PyJWT: JWT encode/decode
# bcrypt: password hashing (SHA256 pre-hash handles the 72-byte limit natively — see Security.py)
PyJWT==2.12.1
bcrypt==5.0.0
python-multipart==0.0.20
slowapi==0.1.9

# PostgreSQL / SQLAlchemy
# alembic is NOT included here — migrations run in the dino-system service only
sqlalchemy[asyncio]==2.0.49
asyncpg==0.31.0

# Environment
python-dotenv==1.0.1

# Email validation (required by pydantic EmailStr)
email-validator==2.2.0

# QR code generation
qrcode[pil]==8.0

# Google Cloud Storage
google-cloud-storage==3.1.0